# This config was automatically generated from your source code
# Stacks detected: deps:node:.,deps:python:.,package_manager:poetry:.,package_manager:yarn:
version: 2.1
jobs:
  build-node:
    # Build node project
    docker:
      - image: cimg/node:16.20.2
    steps:
      - checkout
      - run:
          command: cd /joker-web-app/client
      - run:
          command: yarn install
      - run:
          name: Run react jest unit tests
          command: yarn test a
workflows:
  build-and-test:
    jobs:
      - build-node
    # - deploy:
    #     requires:
    #       - build-node
