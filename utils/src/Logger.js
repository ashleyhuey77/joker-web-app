/*
import chalk from 'chalk';

class Logger {
  constructor() {
    this.name = name;
  }

  static log(level, message) {
    const output = `${new Date().toISOString().replace('T',' ').split('.')[0]} ${this.name} ${level} ${message}`
    console.log(output)
  }

  static info(message) {this.log(chalk.cyanBright('info', message))}
  static debug(message) {this.log(chalk.blackBright('debug', message))}
  static trace(message) {this.log(chalk.magentaBright('trace', message))}
  static warn(message) {this.log(chalk.yellow('warn', message))}
  static error(message) {this.log(chalk.redBright('error', message))}

}

export default Logger;
*/
